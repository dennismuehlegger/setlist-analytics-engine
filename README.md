# Setlist Analytics Engine
Spring Boot REST API modeling a concert setlist data processing pipeline. The system runs automated background workers to get tour data from the Setlist.fm API, processes nested JSON structures, and exposes calculated performance metrics.
